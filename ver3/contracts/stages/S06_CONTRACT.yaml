# STAGE 06 CONTRACT — parameter resolution, and the convergence block
#
# s06 is a DETERMINISTIC SOLVER SERVICE, not a sequential barrier. It may be
# invoked by s05 repeatedly within one stage attempt. Numbering and both
# must_exist lists are preserved: the frozen Oracle stage_expectations for s05
# and s06 are satisfied at CONVERGENCE, because they state what must EXIST, not
# what order it is produced in.

schema_version: "0.1.0"
# ---------------------------------------------------------------- S-2 closure
# AUTHORITY STATUS of each semantic section. See CONTRACT_AUTHORITY.yaml.
# A *_CONTRACT.yaml file is not a semantic authority by default.
authority_status:
  engineering_question:
    class: OPERATIONAL
    note: >
      Stage-local prose or detail. It EXPLAINS; it does not define. Not a
      projection: a paraphrase of a canonical list is not equality, and
      treating it as one would make this file a second semantic authority
      by resemblance rather than by declaration.
    authoritative_for_canonical_semantics: false
    see_canonical_note: >
      No canonical stage responsibility exists for s06: the frozen
      architecture defines s01, s02, s03a, s03b, s04a, gate and s04b
      only. Inventing one to satisfy a projection test would create a
      fact rather than check one.
  engineering_responsibility:
    class: OPERATIONAL
    note: >
      Stage-local prose or detail. It EXPLAINS; it does not define. Not a
      projection: a paraphrase of a canonical list is not equality, and
      treating it as one would make this file a second semantic authority
      by resemblance rather than by declaration.
    authoritative_for_canonical_semantics: false
    see_canonical_note: >
      No canonical stage responsibility exists for s06: the frozen
      architecture defines s01, s02, s03a, s03b, s04a, gate and s04b
      only. Inventing one to satisfy a projection test would create a
      fact rather than check one.
  prohibited_decisions:
    class: OPERATIONAL
    note: >
      Stage-local prose or detail. It EXPLAINS; it does not define. Not a
      projection: a paraphrase of a canonical list is not equality, and
      treating it as one would make this file a second semantic authority
      by resemblance rather than by declaration.
    authoritative_for_canonical_semantics: false
    see_canonical_note: >
      No canonical stage responsibility exists for s06: the frozen
      architecture defines s01, s02, s03a, s03b, s04a, gate and s04b
      only. Inventing one to satisfy a projection test would create a
      fact rather than check one.
  required_inputs:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  prohibited_inputs:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  owned_decisions:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  structured_outputs:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  convergence_block:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  allowed_unresolved:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  maturity_expectations:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  deterministic_exit_checks:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  next_stage_consumer_requirement:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  llm_role:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  knowledge_base_role:
    class: "OPERATIONAL"
    note: "non-semantic detail; defines no canonical meaning"
    authoritative_for_canonical_semantics: false
  settlement_entry:
    class: "OPERATIONAL"
    note: >
      Unit F. When settlement may be asked at all, and how an absent embodiment
      is told apart from an empty system. Enforced in
      `assy_v3/downstream/canonical_io.settlement_readiness`; explained here.
    authoritative_for_canonical_semantics: false
  physical_closure:
    class: "OPERATIONAL"
    note: >
      Unit G. A settlement is usable only if it builds something: primitive
      dimensions positive, placements resolved, clearances with the right sign.
      Enforced in `downstream/settled_geometry.construction_problems` and
      `clearance_problems`; explained here.
    authoritative_for_canonical_semantics: false
  post_settlement_checks:
    class: "OPERATIONAL"
    note: >
      Unit F. The deterministic re-evaluation of s04 spatial commitments once
      s06 has settled the quantities an s05 envelope was stated in. Enforced in
      `assy_v3/downstream/settled_geometry`; explained here.
    authoritative_for_canonical_semantics: false
stage_id: s06
status: draft
governed_by: ver3/phase0/ARCHITECTURE_INVARIANTS.yaml
derived_from: docs/PIPELINE_IMPLEMENTATION_PROPOSAL.md

engineering_question: >
  Do values exist that satisfy every constraint, and if not, exactly which
  constraints conflict?

engineering_responsibility: >
  Solve, or report precisely why not. Never approximate a report of success.

required_inputs:
  from_s05: [Constraint, Parameter declarations]
prohibited_inputs: [Requirement, Candidate, raw source text]

owned_decisions:
  creates: []
  extends: [Parameter values, Constraint status]
  also_owns: ["the promotion of PROVISIONAL placements to AUTHORITATIVE on convergence"]

prohibited_decisions:
  - what: "default a missing unit"
    retirement_row: R-21
    invariant: INV-004
  - what: "report feasible for an underdetermined system"
  - what: "silently pick one member of a solution family"
  - what: "defer a symbolic expression to CAD"
    retirement_row: R-22
  - what: "copy existing values and call them solved"
    retirement_row: R-23
  - what: "any LLM involvement"
    rationale: "an LLM here re-introduces R-23"

structured_outputs:
  per_parameter: [value_or_status, maturity]
  solver_report: [residuals, active_set, margins, solver_status]
  # Unit F: `not_ready` is reported BEFORE solving, for a branch with no
  # current embodiment program; it is the answer to "settle what?", never a
  # property of a system. A system with no unknowns is `feasible` with
  # `unknown_count` 0, which the report states explicitly.
  solver_status: [feasible, infeasible, underdetermined, unsupported_formulation, solver_failure, not_ready]

settlement_entry:
  principle: >
    Settlement is asked FOR an embodiment. The minimum current s05 basis is
    the branch's standing construction program - at least one
    ConstructionStatement of the committed candidate - together with every
    standing Parameter and Constraint of that branch reading as well-formed IR
    (the write boundary guarantees this for everything written since Unit F).
    Without a program there is nothing the settled numbers would build, and a
    solve over the empty system is not evidence of anything.
  not_ready: >
    A branch with no current construction program yields `not_ready`: no
    solver runs, nothing is written, a CONTRACT_CONDITION failure is recorded
    against s06, and the settlement loop stops. This is the only honest
    reading of "s05 produced nothing" and it never resembles success.
  empty_system: >
    A branch whose program references no Parameter and states no Constraint
    has nothing to settle, and that is a legitimate, explicit result:
    `feasible` with `unknown_count` 0 and an empty settled set. It is told
    apart from `not_ready` by the presence of the program.
  units: >
    The kernel builds in millimetres and turns in degrees. Every construction
    parameter must reduce to exactly that dimension in that unit; a statement
    in another unit is refused at compilation entry and never converted -
    nothing in this repository is an authority on what one unit is worth in
    another.

physical_closure:
  principle: >
    The solver settles numbers; whether the numbers build anything is asked
    deterministically afterwards, and before the kernel. A primitive whose
    settled dimension is not positive, a placement whose origin does not
    resolve, a CLEARANCE constraint whose settled margin is negative: each is
    named with the statement and the number, and compilation is not entered.
    Nothing is clamped, defaulted or re-solved here - an underdetermined or
    contradictory system stays what the solver said it is.

post_settlement_checks:
  principle: >
    s04's arrangement, regions and sweeps are provisional commitments in s04's
    basis; s05 states where a feature's material may be as an expression over
    the parameters it declares; s06 settles those parameters. Only then can
    the deferred question - does the embodiment respect what s04 reserved -
    be asked deterministically, and it is asked in s04's own arithmetic
    (`aabb`, `overlaps`, `excludes_occupancy`).
  evaluable: >
    A feature envelope whose every component resolves to a constant with the
    settled values, in a basis the ReferenceScale can convert (ABSOLUTE, and
    the constant's unit is the basis unit), is compared against every
    FunctionalRegion whose role excludes occupancy. An intrusion is a
    finding, and compilation entry refuses while one stands.
  not_evaluable: >
    A RELATIVE basis, a unit the basis does not state, or a parameter without a
    settled value leaves the check NOT_ESTABLISHED for that feature. No scale
    is invented and no relative coordinate is read as a length. Reported,
    never silently passed.

# =====================================================================
convergence_block:
  members: [s04b, s05, s06]
  name: geometric settlement loop

  termination:
    rule: >
      Terminate on a ROUND BUDGET N, or on a REPEATED STATE, where a state is
      the pair (unsatisfied-constraint set, changed-parameter set). A repeated
      state is a genuine cycle and is reported as one.
    explicitly_not: >
      NOT monotone reduction of the unsatisfied set. That rule was proposed and
      is WRONG: replay BM-003 R1 to R4 and round 1 fixes the arm-to-clevis
      clearance while round 2 breaks the lift-only property, leaving the set the
      same size. A monotone rule terminates a problem that has a solution and
      that was actually solved by changing two parameters. Real constraint
      systems are not monotone.

  scope:
    may_change: [placements, dimensions, feature alternatives]
    may_not_change: [Body, RigidGroup, Joint, interaction_kind, MobilityExpectation, LoadPath, AssemblyStep]
    on_may_not_change: >
      ESCALATE to s03, recorded as a FailureProvenance naming the owning
      decision. This is the line that stops the loop meaning "redo the whole
      design".
    validation_against_the_record: >
      Applied to the manual history it reproduces the severity judgements without
      judgement: BM-002 CHG-01 and CHG-03 are iterations; CHG-02 is an escalation
      (it required an overhung crank); BM-003 R1 and R4 are iterations; R2 is an
      escalation (a stop pad realizes a previously undispositioned DOF).

  budgets:
    inner: "at most K feature alternatives per unsatisfied constraint"
    outer: "at most N solver rounds"
    exhaustion_ladder: [feature_alternative, placement, dimension, escalate_to_s03]
    every_exhausted_alternative: "retained as a RejectedAlternative with its reason"

  invalidation:
    cone_computation: >
      Every check declares its input value set (S04B-C8). A change marks STALE
      exactly those checks whose input set intersects the transitive closure of
      the changed values.
    demonstrated_discrimination: >
      BM-002 records three changes each with an explicit UNCHANGED result:
      "Resulting measured travel: 90.000000 mm, unchanged. Travel is 2R and R was
      not touched." Moving the crank axis invalidates the bottom-dead-centre
      clearance check and provably not the travel check.
    on_promotion: >
      When a PROVISIONAL placement is promoted to AUTHORITATIVE, the cone marks
      every s04b check whose inputs moved as STALE, and those checks are re-run.
      Unchanged checks are PRESERVED, not recomputed.

  non_convergence:
    outcome: >
      Terminate with the solver's own vocabulary - infeasible with a named
      conflicting set, or underdetermined with the free directions - plus a
      FailureProvenance routing to the s03 decision that owns the conflict.
    never: >
      Never terminate by silently accepting the last state. Never produce a
      design from a non-converged block.

  not_in_scope: >
    This contract defines a bounded dependency-driven settlement process. It is
    NOT a scheduler and NOT an agent-control architecture.

allowed_unresolved:
  - "free directions in an underdetermined system, recorded as UnresolvedDecision"

maturity_expectations:
  geometry: SOLVED
  authority: "AUTHORITATIVE on convergence; placements promoted from PROVISIONAL"

deterministic_exit_checks:
  - id: S06-C1
    check: "every Parameter has a value or an explicit non-value status"
  - id: S06-C2
    check: "residuals are within the declared bound for a feasible report"
  - id: S06-C3
    check: "the active set is consistent with the reported solver_status"
  - id: S06-C4
    check: "no unit was defaulted"
  - id: S06-C5
    check: "on convergence, every promoted placement has its dependent checks re-run or explicitly preserved"
  - id: S06-C6
    check: "determinism: identical input produces identical output"

next_stage_consumer_requirement:
  consumer: s07
  needs: [construction program, a value for every symbol it references]
  must_not_need: [requirements, candidates, source text, pose law]

llm_role: NONE
knowledge_base_role: "constraint idioms and their solvability class"
