# source/

`request.txt` goes here: the raw source request, verbatim, and nothing else.

**Not yet written.** BM-003 has no source request and no Oracle. Its subject is
deliberately unchosen — picking it inside the rebuild task would make it a
benchmark shaped by the architecture it is meant to test.

Until it exists, no stage contract may be frozen
(`STAGE_PROGRESSION_CONTRACT.yaml` freeze_rule).
