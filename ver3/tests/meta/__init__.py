"""Meta-tests: tests about the repository's structure and contracts.

These do not test pipeline behaviour, because there is no pipeline behaviour yet.
They test that the boundaries hold and that the contracts are mutually consistent.
"""
